async function name(params) {
    while () {
        salemBer("salem Ernar")
        
        function salemBer(text) {
            alert(text);
        }
        
        
        findPeremeter(8,4)
        findPeremeter(12,10)
        
        function findPeremeter(uzundugu, eni){
            let P = (uzundugu + eni) / 2
            alert("Perimeter: " + P)
        }
    }
}